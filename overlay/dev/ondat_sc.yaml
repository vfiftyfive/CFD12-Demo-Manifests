apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: ondat-replicated
provisioner: csi.storageos.com
allowVolumeExpansion: true
parameters:
  #local development environment requires 0 replica
  storageos.com/replicas: "0" # Labels allowed only when using CSI
  storageos.com/encryption: "false"
